<% content_for :title, "Home" %>

			<%= link_to "Sign in with Facebook", user_facebook_omniauth_authorize_path %>
		<div class="victor"><%= image_tag('bouncey.svg', size: "150x80")  %></div>

		<div class="small_victory_text"><%= image_tag('small_victory_text.svg', size: "300x80") %></div>
			<h3 class="app_description">Do one 5 minute task that <br>
			makes you <span class="underline">better.</span></h3>
				<div class="login_signup">
			<% if user_signed_in? %>
				<h3>How's it going, <%= current_user.name %>?</h3>
			<% else %>
				<h3><span class="underline"><%= link_to("Log in", new_user_session_path) %></span> | <span class="underline"><%= link_to("Sign up", new_user_registration_path) %></span></h3>
				</div>
			<% end %>








